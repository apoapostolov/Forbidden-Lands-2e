# Forbidden Lands Notes

This file stores system-specific exceptions that should not leak into the generic PDF-to-Markdown skill.

## Use rule

Only keep material here if it depends on Forbidden Lands terminology, layout, or chapter structure.

## Examples of project-specific patterns

- spell metadata glyph surrogates in spell blocks
- repeated title fragments tied to this book’s chapter names
- statblock and adventure-site quirks specific to this manuscript set
- known chapter-level corrections that should not become generic OCR rules

## Rule

If a fix works for any RPG PDF with the same structural damage, move the reusable part into the shared references and keep only the book-specific remainder here.
