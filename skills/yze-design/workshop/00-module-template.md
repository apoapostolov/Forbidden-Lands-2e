<!-- markdownlint-disable MD013 -->

# [Module Name] — [One-line description]

> **STATUS: WORKSHOP MODULE.** A new, engine-agnostic subsystem built from proven primitives. The bulk of this file is the *generic design space*; the worked genre example at the end is one calibrated instantiation. Drop into any YZE-family game by setting the dials and swapping the nouns.

## Contents

1. Origin — how this was built
2. The generic mechanism
3. The pressure loop
4. Dials
5. Integration points
6. Failure modes & edge cases
7. Validation notes
8. Worked genre example — [Genre]

## 1. Origin — how this was built

- **Source primitive(s):** P# from `16-mechanical-primitives.md` (e.g. P2 capped metacurrency, P4 typed D66, P5 resource die, P6 activity menu, P7 org lifecycle).
- **Reinvention operator:** which of the five from `18 §4` — Domain Transfer / Inversion / Recalibration / Fusion / Abstraction-Climb.
- **Target psychology:** which cell of `17`'s dual-use matrix (e.g. action-refuel → drama; harm-refuel → aggression).
- **What problem it solves:** the design need this subsystem answers.

## 2. The generic mechanism

*The abstract procedure, stripped of genre nouns. This is the transferable core.*

## 3. The pressure loop

*Which resource is under pressure, what decision the player makes, what consequence follows, what state changes. Name the loop's shape (e.g. "invest → expose → backlash → recover").*

## 4. Dials

*Each tunable parameter, with at least two calibrated settings and the psychology each produces (per `17`).*

## 5. Integration points

*Which engine systems this touches, what it requires to exist, what (if anything) it replaces. A designer installing this module needs to know what it hooks into.*

## 6. Failure modes & edge cases

*Run the module against `13 §5` (exploit taxonomy) and `19 §4` (felt-experience failure modes). Name the specific traps and how to avoid them.*

## 7. Validation notes

*How it passed the pipeline (`13 §8`) and the felt-experience protocol (`19 §7`). Known edge cases and design caveats.*

## 8. Worked genre example — [Genre]

*A concrete instantiation: the dials set, the nouns skinned (per `15`), a brief play example showing it in action. This is a slice — the genre is illustrative, not load-bearing.*
